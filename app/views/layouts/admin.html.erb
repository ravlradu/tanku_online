<!DOCTYPE html>
<html>
<head>
  <title>Patria</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>

<p><!-- this should sit in the header (alxx) -->
  <% if @current_user.nil? %>
    <%= link_to 'Login', admin_index_path %>
  <% else %>
    <%= link_to 'Logout', authentication_logout_path %>
  <% end %>
</p>

<%= yield %>
</body>
</html>
